# Convenience: install the FULL pipeline in one shot with
#     pip install -r requirements.txt
#
# The recommended way is `pip install .` (core) or `pip install ".[full]"`,
# which reads these same dependencies from pyproject.toml. This file is just a
# familiar fallback that mirrors the "[full]" extra.
#
# GPU note: for an NVIDIA GPU, install the CUDA build of torch FIRST, e.g.
#     pip install torch --index-url https://download.pytorch.org/whl/cu124
# then run this file. Otherwise a CPU torch is installed automatically.

# --- core: generate / encode / decode / score / fine-tune / download weights ---
torch>=2.2
rdkit>=2023.9
selfies>=2.1
numpy>=1.24
huggingface_hub>=0.20

# --- training / preprocessing / evaluation ---
tqdm>=4.66

# --- CE inverse-design predictor (ce_*.py) ---
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
optuna>=3.4
joblib>=1.3

# --- web app / local REST API (app.py, server.py, webengine.py) ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# --- natural-language parsing (llm.py; optional, needs Azure/OpenAI keys) ---
openai>=1.40
python-dotenv>=1.0

# --- DFT grounding (qm9.py) + HTML reports (report.py) ---
torch-geometric>=2.5
matplotlib>=3.8

# xTB is an external binary (set config.XTB_EXE), not a pip package.
